---

- name: Initial setup
  hosts: all
  gather_facts: true
  become: true
  tasks:

    - name: Install dev utils
      apt:
        name:
          - git
          - vim
          - htop
          - jq
        state: latest
        update_cache: yes
